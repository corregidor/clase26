<div class="container">
<div class="row">
<div class="col-lg-10">
<div class="well">

	<%= form_with model: @todo, local: true do |t| %>
		<%= t.label :description, "Descripción" %>
		<%= t.text_field :description %>
	    <br><br>
		<%= t.label :completed, "Completado" %>
	    <%= t.select :completed, options_for_select([["si", true], ["no", false]])%>
	    <br><br>
	    <%= t.submit "Enviar" %>
	<% end %>
</div>
</div>
</div>
</div>


